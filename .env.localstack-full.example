# Copy to .env.localstack-full and fill SFN arn after: make tf-apply && make localstack-full-print-env
# Then: docker compose -f docker-compose.yml -f docker-compose.localstack-full.yml --env-file .env.localstack-full up -d

SFN_STATE_MACHINE_ARN=
